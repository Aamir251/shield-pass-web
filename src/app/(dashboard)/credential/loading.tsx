import BasicSpinnerLoader from "@/components/loaders/basic-spinner"

const Loading = () => {
  return (
    <BasicSpinnerLoader className="flex-grow-0 h-full flex-center" />
  )
}

export default Loading